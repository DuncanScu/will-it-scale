[project]
name = "will-it-scale"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Duncan Scuffell", email = "dscuffell@microsoft.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
will-it-scale = "will_it_scale:main"

[build-system]
requires = ["uv_build>=0.12.14,<0.13.0"]
build-backend = "uv_build"
